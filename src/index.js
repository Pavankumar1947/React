import React from "react";
import {createRoot} from "react-dom/client"
import Banking from "./BankingApplication/Banking";
// import App from "./App";

// createRoot(document.getElementById('root')).render(<App/>)
createRoot(document.getElementById('root')).render(<Banking/>)
